/**
 * @file Phase 3 Layout
 * @description Layout wrapper for Phase 3 sub-pages (account, insurance, matching).
 *
 * @see {@link _docs/user-flow.md} Phase 3: Logistics & Matching
 */

/**
 * Phase 3 layout component
 * Provides consistent structure for Phase 3 sub-pages
 */
export default function Phase3Layout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}

